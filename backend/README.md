# Fire-hazard app backend


### One-time setup

Navigate to fire-hazard/backend

Run 
- `python -m venv venv` to create a virtual environment
- `pip install -r requirements.txt` to install all required packages


### Running

> python -m flask run
